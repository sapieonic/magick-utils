{
  "name": "MagickUtils",
  "install": "npm ci",
  "terminals": [
    {
      "name": "dev",
      "command": "npm run dev",
      "description": "Next.js 16 dev server (Turbopack) on http://localhost:3000. Runs on seeded mock data with zero env config; set backend/LLM/Firebase env vars to wire live data."
    }
  ],
  "ports": [
    { "name": "web", "port": 3000 }
  ]
}
