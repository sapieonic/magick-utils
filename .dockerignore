# Dependencies & build output (reinstalled/rebuilt inside the image)
node_modules
.next
out
build
coverage

# Local env & secrets — provided at runtime, never baked into the image
.env
.env.*
!.env.example

# VCS & tooling
.git
.gitignore
.github
.vscode
.idea
.claude

# Tests & docs (not needed to build or run)
tests
**/*.test.ts
**/*.test.tsx
*.md

# Docker & misc
Dockerfile
.dockerignore
npm-debug.log*
.DS_Store
